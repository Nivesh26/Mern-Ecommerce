# MongoDB connection string (get from https://cloud.mongodb.com or use local)
MONGO_URI=mongodb://localhost:27017/nivesh

# Secret for JWT tokens (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server port
PORT=5000
